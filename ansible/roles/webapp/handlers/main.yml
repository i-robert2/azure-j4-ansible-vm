---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart gunicorn
  ansible.builtin.systemd:
    name: gunicorn
    state: restarted

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
